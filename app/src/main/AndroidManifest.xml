<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".controller.NumbersliderActivity"
            android:exported="false"
            android:label="@string/title_activity_numberslider" />
        <activity
            android:name="com.example.myapplication.visualization.Activity_2048"
            android:exported="false"
            android:label="@string/title_activity_2048" />
        <activity
            android:name=".controller.TicTacToeActivity"
            android:exported="false"
            android:label="@string/title_activity_tic_tac_toe"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".controller.MainActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".controller.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>